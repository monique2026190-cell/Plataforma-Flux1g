services:
  - type: web
    name: flux-platform
    env: node
    plan: free
    rootDir: .
    buildCommand: npm install && npm run build && npm run build:server
    startCommand: node dist/server.js
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
      - key: NODE_ENV
        value: production
