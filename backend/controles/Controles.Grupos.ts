
import { Request, Response } from 'express';

const criarGrupo = async (req: Request, res: Response) => {
    // Lógica para criar um grupo
};

const obterGrupos = async (req: Request, res: Response) => {
    // Lógica para obter grupos
};

export default { criarGrupo, obterGrupos };
